/**
 * Stability scoring regex patterns.
 */

export const ID_SELECTOR_PATTERN = /^#[\w-]+$/;
export const TAG_CLASS_SELECTOR_PATTERN = /^[\w-]+\.[\w-]+/;
export const TAG_ONLY_SELECTOR_PATTERN = /^[\w-]+$/;
